// Este arquivo é obsoleto e não é mais utilizado pela aplicação (o roteamento principal é feito via TanStack Router em src/main.tsx).
// Mantido com export dummy para não quebrar a build do TypeScript.
export function App() {
  return null
}
